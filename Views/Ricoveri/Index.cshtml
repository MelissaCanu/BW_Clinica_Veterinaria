@model IEnumerable<BW_Clinica_Veterinaria.Models.Ricoveri>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Animali.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DataIN)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DataOUT)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Animali.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DataIN)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DataOUT)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RicoveroID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RicoveroID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RicoveroID })
        </td>
    </tr>
}

</table>
